[pytest]
filterwarnings =
    error
    ignore::DeprecationWarning

markers =
    skip_dependency_tests: Skips tests that involves calculating dependencies

; asyncio_mode = auto

xfail_strict = true
